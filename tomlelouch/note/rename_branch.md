# change branch name *tomlelouch* --> **mba_tom**

```
git branch -m tomlelouch mba_tom
git push origin :tomlelouch      # delete remote tomlelouch
git push origin mba_tom          # create mba_tom on remote
```
